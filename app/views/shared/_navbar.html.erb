<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= image_tag "pngwing.com.png", size: "60x60" %>
    <%= link_to "Chess Quest", root_path, class: "navbar-brand pl-3" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
           <!-- DASHBOARD PATH -->
          </li>
        <% end %>
        <li class="nav-item">
        </li>
      </ul>

      <ul class="nav navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, class: "nav-link"%>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In With Google', user_google_oauth2_omniauth_authorize_path, method: :post, class: "nav-link" %>
          </li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>
<br>